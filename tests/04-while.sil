(#include "../src/stdlib/_.sil")

(#extern puts (#types/func [#types/string #types/int]))

(#define main (#func [() (#types/int)] (#do {
    (#define x 10)
    (#while
        (#do {
            (#define y x)
            (#set x (Int_dec x))
            y
        })
        (#do {
            (puts "TEST\n")
        })
    )
    0
})))
